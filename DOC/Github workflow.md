Recommended Future Improvements

Later you should add:

Feature	Purpose
release signing	Windows trust
auto GitHub releases	automated publishing
semantic versioning	version management
cache Rust dependencies	faster builds
cache Bun dependencies	faster frontend builds

tauri build --release
bun tauri build